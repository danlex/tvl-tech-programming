console.log("\nExercise 1: Weekly report\n");

const completedTasks: number = 7;
const blockedTasks: number = 2;

function buildWeeklyMessage(completed: number, blocked: number): string {
  // Replace this placeholder with a real message.
  return "TODO";
}

const report = buildWeeklyMessage(completedTasks, blockedTasks);
console.log(report);

console.log("\nGoal:");
console.log("- Return a sentence using both numbers");
console.log('- Example: "We completed 7 tasks and 2 are blocked."');
